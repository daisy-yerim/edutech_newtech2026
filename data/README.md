# Data

`data/`에는 원천 자료와 기준 데이터만 둡니다.

- `sources/`: 공공자료, TOPIK 샘플, 연구자료 같은 원문 출처
- `reference/`: 어휘 등급표, 문법 기준, 사전 DB 같은 참조 데이터
- `criteria/`: 생성 기준, 평가 기준, 채점 규칙
- `samples/`: 예시 PDF 자료
- `runtime/`: 로컬 실행 중 생성되는 데이터

`runtime/`는 로컬 전용입니다. SQLite 파일과 로그, 업로드 파일은 저장소에 올리지 않습니다.

